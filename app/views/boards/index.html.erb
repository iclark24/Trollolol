<h1>Boards</h1>
<%= link_to "New Board", new_board_path, class:"ui green button inverted"%>
<hr/>
<div class="ui grid">
  <% @boards.each do |board| %>
    <div class="three wide column">
      <div class="ui raised center aligned segment">
        <h3><%= link_to board.b_name, board %></h3>
      </div>
    </div>
  <% end %>
</div>